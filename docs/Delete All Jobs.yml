info:
  name: Delete All Jobs
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/api/jobs"
  headers:
    - name: X-App-ID
      value: "{{app_id}}"
    - name: Authorization
      value: Bearer {{jwt_token}}
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
