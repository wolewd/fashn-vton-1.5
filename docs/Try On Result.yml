info:
  name: Try On Result
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/tryon/:uuid"
  headers:
    - name: X-App-ID
      value: "{{app_id}}"
    - name: Authorization
      value: Bearer {{jwt_token}}
  params:
    - name: uuid
      value: 9074a9c2-bd40-49b5-9891-e85932dcff8b
      type: path
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
